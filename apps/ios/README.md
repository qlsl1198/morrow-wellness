# Morrow iPhone

SwiftUI iPhone 앱 소스입니다. Xcode iOS 타깃에 `Sources`를 추가하고 HealthKit capability를 활성화하세요.

- `DesignSystem/` 디자인 토큰과 부하 레벨 시맨틱 컬러
- `Views/` 대시보드, 상태 기록 화면과 재사용 컴포넌트(`Views/Components/`)
- `Models/`, `Services/` 웰니스 모델, HealthKit 연동, 기준선 분석
